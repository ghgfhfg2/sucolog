## [TOOLOG_WEBTOOL_BUILD] 00:00 KST
- 배포 툴: **물타기 계산기** (`/tools/stock-average-calculator/`)
- 구현 내용:
  - 현재 보유 수량/평단 + 추가 매수 수량/가격 기반 새 평단가 계산
  - 목표 평단가 입력 시 달성 필요 추가 수량 역산
  - 달성 불가 조건(목표 평단 ≤ 추가 매수가) 예외 처리
  - 결과 복사/초기화 UX 포함
- SEO 반영:
  - 전용 랜딩 문서 `_tools/stock-average-calculator.md` 작성
  - title/description/keywords/FAQ/related_tools 메타 및 본문 섹션 구성
  - 썸네일 추가 `assets/thumbs/stock-average-calculator.svg`
- 사이트 반영 파일:
  - `_layouts/tool.html` (런타임 UI 케이스 추가)
  - `assets/js/tools.js` (계산 로직 추가)
  - `_data/tools.yml` (홈/목록 노출 등록)
- 검증:
  - `bundle exec jekyll build` 시도 → 로컬에 `bundle` 미설치로 실행 불가
- Git:
  - Commit: `01a724d` (`feat: add stock average calculator web tool`)
  - Push: `main` 브랜치 반영 완료

## [TOOLOG_WEBTOOL_IMPROVE] 00:30 KST
- 점검 범위: 동작/예외/UX/SEO 재검토 (물타기 계산기)
- 개선 사항:
  - 목표 평단 필요수량 계산을 **기존 보유 기준**에서 **현재 입력된 추가 매수 계획 반영 기준**으로 수정
    - `need = ceil((총매수금액 - 목표평단*총수량) / (목표평단-추가매수가))`
  - 이미 목표를 만족하는 케이스에서 안내 문구를 상황별(기존 보유 vs 추가계획 반영 후)로 분리
  - UI 라벨을 `목표 평단 필요 추가수량` → `목표 평단까지 남은 추가수량`으로 변경
  - 랜딩 문서 핵심 기능 설명도 계산 기준과 일치하도록 업데이트
- 검증:
  - 정적 로직 점검 완료
  - `bundle exec jekyll build` 재시도 불가 (로컬 `bundle` 미설치)
- Git:
  - Commit: `3be4b4e` (`Fix stock average target qty to account for planned buys`)
  - Push: `main` 브랜치 반영 완료
- [01:00] Toolog: '물타기 계산기 핵심 로직' 포스트 발행 완료. SEO 메타/내부링크 반영, commit a8cddfe, push 완료.

## [TOOLOG_WEBTOOL_BUILD] 08:00 KST
- 배포 툴: **칼로리 계산기 (TDEE)** (`/tools/tdee-calculator/`)
- 수요 선정 근거:
  - 다이어트/벌크 시즌성 + 상시 검색 수요가 높은 기초 건강 계산 카테고리 보강 목적
  - 기존 BMI 도구와 연계 가능한 확장성(체중 상태 → 섭취 칼로리 전략)
- 구현 내용:
  - 성별/나이/키/몸무게/활동량 입력 기반 **BMR(TDEE) 계산**
  - 감량(보수/적극), 유지, 증량(린/적극) 권장 칼로리 동시 제시
  - 유지 칼로리 기준 단백질/지방/탄수화물 매크로 가이드 계산
  - 결과 복사/초기화 UX 포함
- SEO 반영:
  - 전용 랜딩 문서 `_tools/tdee-calculator.md` 작성
  - title/description/keywords/FAQ/related_tools 최적화
  - 썸네일 추가 `assets/thumbs/tdee-calculator.svg`
  - 코어 로직 포스트 발행 `_posts/2026-03-05-tdee-calculator-core-logic.md`
- 사이트 반영 파일:
  - `_layouts/tool.html` (런타임 UI 케이스 추가)
  - `assets/js/tools.js` (계산 로직 추가)
  - `_data/tools.yml` (홈/목록 노출 등록)
- 검증:
  - `node --check assets/js/tools.js` 통과
  - `_data/tools.yml` YAML 파싱 확인 (PyYAML)
  - `bundle exec jekyll build` 시도 → 로컬 `bundle` 미설치로 실행 불가
- Git:
  - Commit: `0b38c0f` (`Add TDEE calorie calculator tool with SEO page and core logic post`)
  - Push: `main` 브랜치 반영 완료

## [TOOLOG_WEBTOOL_IMPROVE] 08:30 KST
- 대상: 칼로리 계산기 (TDEE) 30분 재점검
- 점검 범위: 동작/예외/UX/SEO
- 개선 사항:
  - UX: 결과 안내문에 **주간 예상 변화 속도**(보수 감량, 린 증량) 추가
    - `weeklyCutKg = ((tdee - tdee*0.85) * 7) / 7700`
    - `weeklyBulkKg = ((tdee*1.06 - tdee) * 7) / 7700`
  - 예외 안내: 단백질/지방 최소치 합이 유지 칼로리를 거의 채우는 경우, 탄수화물 권장량이 낮게 표시될 수 있다는 문구 추가
  - SEO/콘텐츠 동기화:
    - `_tools/tdee-calculator.md` 핵심 기능에 주간 변화 속도 안내 항목 반영
    - `_posts/2026-03-05-tdee-calculator-core-logic.md`에 30분 개선 포인트 섹션 추가
- 검증:
  - `node --check assets/js/tools.js` 통과
- Git:
  - Commit: `28167c3` (`Improve TDEE UX with weekly pace guidance and macro notice`)
  - Push: `main` 브랜치 반영 완료

## [TOOLOG_WEBTOOL_LOGIC_POST] 09:00 KST
- 대상: 직전 추가 툴 **칼로리 계산기(TDEE)**
- 수행:
  - 코어 로직 포스트 발행 상태 확인 (`_posts/2026-03-05-tdee-calculator-core-logic.md`)
  - SEO 요소(title/description/tags) 점검 완료
  - 내부 링크(도구 본문 + BMI/만나이 연계) 점검 완료
  - 본문 검수 완료 (공식/예외/개선 포인트 포함)
- 결과:
  - 추가 수정 필요사항 없음 (기발행본 기준 충족)
  - Git 신규 커밋 없음 (기존 반영분 `0b38c0f`, `28167c3`에 포함)
- 보고:
  - 개별 보고 생략, 일일 종합 보고에 포함 예정

## [TOOLOG_WEBTOOL_BUILD] 16:00 KST
- 배포 툴: **비밀번호 생성기** (`/tools/password-generator/`)
- 수요 선정 근거:
  - 계정 보안/회원가입/관리자 계정 생성 등 범용 수요가 매우 높은 필수 유틸 카테고리
  - 기존 텍스트/계산 중심 툴셋에 보안 카테고리 확장 목적
- 구현 내용:
  - 길이(4~128), 생성 개수(1~20) 기반 랜덤 비밀번호 생성
  - 대문자/소문자/숫자/특수문자 선택 조합 및 헷갈리는 문자 제외 옵션
  - 선택한 문자 유형이 각 비밀번호에 최소 1개 이상 포함되도록 생성 보장
  - 강도 추정/문자풀 크기/가능 조합수(근사)/엔트로피(bit) 표시
  - 전체 결과 복사 UX 제공
- SEO 반영:
  - 전용 랜딩 문서 `_tools/password-generator.md` 작성
  - title/description/keywords/FAQ/related_tools 구성
  - 썸네일 추가 `assets/thumbs/password-generator.svg`
- 사이트 반영 파일:
  - `_layouts/tool.html` (런타임 UI 케이스 추가)
  - `assets/js/tools.js` (생성 로직 + 강도 계산 + 복사 처리)
  - `_data/tools.yml` (홈/목록 노출 등록)
- 검증:
  - `node --check assets/js/tools.js` 통과
  - `bundle exec jekyll build` 시도 → 로컬 `bundle` 미설치로 실행 불가
- Git:
  - Commit: `445202c` (`Add password generator tool with SEO metadata and UI`)
  - Push: `main` 브랜치 반영 완료

## [TOOLOG_WEBTOOL_IMPROVE] 16:30 KST
- 대상: 비밀번호 생성기 30분 재점검
- 점검 범위: 동작/예외/UX/SEO
- 개선 사항:
  - 생성 결과 **중복 최소화 로직** 추가 (고유 비밀번호 우선 생성, 조합 한계 시 생성 개수 안내)
  - 조합 수 표시 계산을 `Math.pow` 직접 계산 방식에서 **log10 기반 표기**로 변경해 대형 값(오버플로우 구간)에서도 안정적으로 표시
  - 헷갈리는 문자 제외 대상 확장 (`B/8`, `S/5`, `Z/2` 포함)
  - 길이가 선택 문자유형 수보다 작은 경우 예외 안내 문구 추가
  - 랜딩 문서(`_tools/password-generator.md`)의 핵심 기능 설명을 개선 내용과 동기화
- 검증:
  - `node --check assets/js/tools.js` 통과
- Git:
  - Commit: `be96994` (`Improve password generator uniqueness and entropy UX`)
  - Push: `main` 브랜치 반영 완료

## [TOOLOG_WEBTOOL_LOGIC_POST] 17:00 KST
- 대상: 직전 추가 툴 **비밀번호 생성기**
- 수행:
  - 코어 로직 포스트 신규 작성/발행: `_posts/2026-03-05-password-generator-core-logic.md`
  - SEO 점검: title/description/categories/tags 반영
  - 내부 링크 반영: 비밀번호 생성기 본문 + text-counter/case-converter 연결
  - 본문 검수: CSPRNG, 최소 1개 포함 보장, 셔플, 엔트로피 계산, 예외 처리 흐름 점검
- Git:
  - Commit: `c94de8d` (`Publish password generator core logic post`)
  - Push: `main` 브랜치 반영 완료
- 보고:
  - 개별 보고 생략, 일일 종합 보고에 포함 예정
