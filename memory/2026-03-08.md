# 2026-03-08

## TOOLOG_WEBTOOL_BUILD (00:00 KST)
- 배포 툴: **Tip Calculator (팁 계산기)**
- 커밋: `504e9fa` (main push 완료)
- 배포 URL 경로:
  - `/tools/tip-calculator/`
  - `/en/tools/tip-calculator/`
  - `/ja/tools/tip-calculator/`

### 핵심 변경사항
- 신규 도구 문서 추가
  - `_tools/tip-calculator.md` (KO)
  - `en/tools/tip-calculator.md` (EN)
  - `ja/tools/tip-calculator.md` (JA)
- 도구 UI/로직 추가
  - `_layouts/tool.html`: `tip-calculator` 렌더링 케이스 추가
  - `assets/js/tools.js`: 팁 계산 로직 추가
    - 팁 기준: 기본금액 only / 세금+서비스 포함
    - 팁 모드: 비율(%) / 고정 금액
    - 결과: 팁 금액, 총 결제금액, 1인당 금액, 팁 기준 금액
    - 결과 복사/초기화/입력 검증 포함
- 사이트 노출 데이터 추가
  - `_data/tools.yml`: 다국어 메타(title/description/url) 등록
- 썸네일 추가
  - `assets/thumbs/tip-calculator.svg`
- SEO/콘텐츠 보강
  - `_posts/2026-03-08-tip-calculator-core-logic.md`

### 검증
- `node --check assets/js/tools.js` 통과
- 로컬 Jekyll 빌드 도구(`bundle`) 부재로 정적 빌드는 미실행

## TOOLOG_WEBTOOL_IMPROVE +30m (cron:71717773-bd94-438c-8be1-afa10b00882a, 00:30 KST)
- 대상: 직전 배포 툴 **팁 계산기** (`tip-calculator`)
- 재점검 범위: 동작/예외/UX/SEO

### 변경사항
- 파일: `toolog/assets/js/tools.js`
- 입력 예외/경계값 보강:
  - 숫자 파싱을 `Number.isFinite` 기반으로 보강해 비정상 문자열 입력 시 `NaN` 전파 차단
  - 인원수 `1~1000` 범위로 자동 보정(소수/비정상 입력 포함)
  - 팁 비율 `0~100%` 자동 보정
  - 고정 팁 금액 상한 클램프 적용(과도한 값 방어)
  - 보정된 값은 입력 필드에 즉시 반영해 계산/표시 일관성 유지

### Git
- Commit: `5be55e3` (`Harden tip calculator input sanitization and bounds`)
- Push: `origin/main` 반영 완료

### 검증 메모
- JS 문법 체크 통과: `node --check assets/js/tools.js`
- 로컬 Jekyll 빌드는 `bundle` 부재로 미실행
- 개별 보고 생략, 일일 종합 보고에 포함 예정

## TOOLOG_WEBTOOL_LOGIC_POST +60m (cron:00c2d6c1-fdf6-467b-a21a-54a4c39857d8, 01:00 KST)
- 대상: 직전 추가 툴 **팁 계산기** (`tip-calculator`)
- 목표: 코어 로직 설명글 고도화(작성/SEO/내부링크/검수)

### 변경사항
- 파일: `toolog/_posts/2026-03-08-tip-calculator-core-logic.md`
- 기존 영문 단문 포스트를 **구조화된 한국어 코어 로직 글**로 개편
  - 핵심 로직 6단계(기본 청구금액 → 팁 기준금액 분기 → 퍼센트/고정 모드 → 1인당 정산 → clamp 방어 → 복사 문구)로 재구성
  - 검색 의도 반영 키워드 강화(팁 계산기, 더치페이, 입력값 검증, 정산)
  - 메타데이터 보강(title/description/categories/tags/date)
  - 내부 링크 추가: 팁 계산기, 퍼센트 계산기, 할인율 계산기, 금융 카테고리

### Git
- Commit: `b1888ce` (`Polish tip calculator core-logic post for SEO and internal linking`)
- Push: `origin/main` 반영 완료

### 검수 메모
- Front matter/링크 문법 재확인
- 로컬 Jekyll 정적 빌드는 `bundle` 부재로 미실행
- 개별 보고 생략, 일일 종합 보고에 포함 예정

## TOOLOG_WEBTOOL_BUILD (cron:283c6d7c-4358-4bea-b65d-307f50b0b67d, 08:00 KST)
- 사이클 완료: 실사용 웹툴 1종 사이트 추가/발행
- 선정 툴: **할인 계산기** (`discount-calculator`)
- 커밋: `bbef4de` (main push 완료)
- 배포 URL 경로:
  - `/tools/discount-calculator/`
  - `/en/tools/discount-calculator/`
  - `/ja/tools/discount-calculator/`

### 핵심 변경사항
- 사이트 노출 데이터 추가
  - `toolog/_data/tools.yml`
  - `discount-calculator` 다국어 메타(title/description/url/category/thumbnail) 등록
- SEO 보강 콘텐츠 발행
  - `toolog/_posts/2026-03-08-discount-calculator-checkout-guide.md`
  - 쿠폰·수량·배송비 포함 최종 결제금액 계산 로직 중심 키워드형 가이드 작성
  - 내부 링크 연결(퍼센트 계산기/부가세 계산기/대출 계산기)

### 검증
- `_data/tools.yml` YAML 파싱 검증 통과(PyYAML)
- `discount-calculator` ID 등록/인덱스 확인 완료
- Git commit/push 완료 (`origin/main`)

### 보고
- 개별 보고 생략, 일일 종합 보고에 포함 예정

## TOOLOG_WEBTOOL_IMPROVE +30m (cron:71717773-bd94-438c-8be1-afa10b00882a, 08:30 KST)
- 대상: 직전 배포 툴 **할인 계산기(사이드 영향 확인 포함: 팁 계산기)**
- 재점검 범위: 동작/예외/UX/SEO

### 점검 결과
- 동작/예외: 할인 계산기 기존 계산 로직은 이상 없음(입력/모드 전환/결과 계산 흐름 유지)
- UX/일관성: 팁 계산기에서 "통화 고정 없음" 안내와 실제 출력 포맷 간 불일치 발견
  - EN/JA 페이지 본문은 currency-agnostic 사용을 안내하지만, 결과값에는 `$`/`円` 접미가 고정되어 있어 혼동 여지
- SEO: 이번 수정은 메타/URL 변경 없는 프런트 로직 일관성 개선으로 인덱싱 영향 미미

### 변경사항
- 파일: `toolog/assets/js/tools.js`
- 수정: `tip-calculator`의 결과 포맷을 로케일 숫자만 표시하도록 변경
  - 기존: 언어별 고정 통화 기호/단위(`$`, `円`, `원`) 부여
  - 변경: `toLocaleString` 숫자 표시만 사용(통화 중립 계산기 UX와 문구 일치)

### Git
- Commit: `971dd28` (`Make tip calculator outputs currency-agnostic across locales`)
- Push: `origin/main` 반영 완료

### 검증 메모
- JS 문법 체크 통과: `node --check assets/js/tools.js`
- 로컬 Jekyll 빌드는 `bundle` 부재로 미실행
- 개별 보고 생략, 일일 종합 보고에 포함 예정

## TOOLOG_WEBTOOL_LOGIC_POST +60m (cron:00c2d6c1-fdf6-467b-a21a-54a4c39857d8, 09:00 KST)
- 대상: 직전 추가 툴 **할인 계산기** (`discount-calculator`)
- 목표: 코어 로직 설명글 발행(작성/SEO/내부링크/검수)

### 변경사항
- 파일: `toolog/_posts/2026-03-08-discount-calculator-core-logic.md` (신규)
- 반영 내용:
  - 할인 계산 흐름을 5단계로 구조화(입력 정규화 → 할인율 적용 → 쿠폰 차감 → 수량/배송비 반영 → 실질 할인율/역산)
  - 검색 의도 키워드 강화(할인 계산기, 쿠폰 계산, 최종 결제금액, 실질 할인율, 역산 할인율)
  - 내부 링크 추가: 할인 계산기, 퍼센트 계산기, 부가세 계산기, 대출 계산기, 쇼핑 카테고리
  - Front matter SEO 메타(title/description/categories/tags/date) 보강

### Git
- Commit: `213e28d` (`Publish discount calculator core logic post with SEO and internal links`)
- Push: `origin/main` 반영 완료

### 검수 메모
- Front matter/내부 링크 문법 재확인
- 로컬 Jekyll 빌드는 `bundle` 부재로 미실행
- 개별 보고 생략, 일일 종합 보고에 포함 예정

## [TOOLOG_WEBTOOL_BUILD] 16:00 KST cycle
- 선정 툴: **체지방률 계산기 (US Navy 공식)**
- 배포 상태: 구현/커밋/푸시 완료
- 커밋: `17dc15b` (toolog main)
- 핵심 변경
  - 신규 도구 페이지 추가: `/_tools/body-fat-calculator.md`
  - 다국어 페이지 추가: `/en/tools/body-fat-calculator.md`, `/ja/tools/body-fat-calculator.md`
  - 도구 런타임 UI 추가: `_layouts/tool.html` (`tool_key: body-fat-calculator`)
  - 계산 로직 추가: `assets/js/tools.js`
    - 남/여 US Navy 체지방률 공식
    - 체지방량/제지방량 계산
    - 범주 판정 + 복사/리셋
  - 목록/탐색 반영: `_data/tools.yml`
  - 썸네일 추가: `assets/thumbs/body-fat-calculator.svg`
- 검증
  - JS 문법 확인: `node -e "new Function(...tools.js)"` 통과
  - 로컬 Jekyll build는 `bundle` 미설치로 미실행

## [TOOLOG_WEBTOOL_IMPROVE] 16:30 KST cycle
- 대상: 직전 배포 툴 **체지방률 계산기** (`body-fat-calculator`)
- 점검 범위: 동작 / 예외 / UX / SEO

### 점검 결과
- 동작: 남/여 전환 및 계산/복사/리셋 흐름 정상
- 예외: 입력값 범위 초과/비정상 값에 대한 명시적 방어 로직 보강 필요 확인
- UX: 남성 모드에서도 힙 입력창이 활성 상태여서 혼동 여지 확인
- SEO: 메타/URL/콘텐츠 구조는 현행 유지 (이번 개선은 UI/로직 안정화 중심)

### 변경사항
- 파일: `toolog/assets/js/tools.js`
- 개선 내용:
  - 체지방 계산 입력 범위 검증 추가
    - 키 120~230, 목 20~70, 허리 40~180, 힙 50~200(여성), 몸무게 30~250
    - 범위 밖 입력 시 계산 중단 + 안내 문구(`invalid`) 노출
  - 성별 전환 UX 개선
    - 남성 모드에서 힙 입력 필드 비활성화(`disabled`/`aria-disabled`)
    - 남성 모드 전환 시 힙 값 초기화로 불필요 입력 혼선 제거
    - 초기 진입/리셋 시에도 동일 상태 일관 적용

### Git
- Commit: `d0a1283` (`Improve body-fat calculator UX and input validation`)
- Push: `origin/main` 반영 완료

### 검증 메모
- JS 문법 체크 통과: `node --check assets/js/tools.js`
- 로컬 Jekyll 빌드: `bundle` 명령 부재로 미실행
- 개별 보고 생략, 일일 종합 보고에 포함 예정

## [TOOLOG_WEBTOOL_LOGIC_POST] 17:00 KST cycle
- 대상: 직전 추가 툴 **체지방률 계산기** (`body-fat-calculator`)
- 목표: 코어 로직 설명글 발행(작성/SEO/내부링크/검수)

### 변경사항
- 파일: `toolog/_posts/2026-03-08-body-fat-calculator-core-logic.md` (신규)
- 반영 내용:
  - US Navy 공식 로직을 남/여 분기 중심으로 6단계 구조화
  - 검색 의도 키워드 강화(체지방률 계산기, US Navy 공식, 체지방량/제지방량, 입력 검증)
  - 입력 범위 검증·로그 정의역 방어·결과값 필터링 등 안정성 로직 설명
  - 내부 링크 추가: 체지방률 계산기, BMI 계산기, TDEE 계산기, 건강 카테고리
  - Front matter SEO 메타(title/description/categories/tags/date) 보강

### Git
- Commit: `c0bf9d1` (`Publish body-fat calculator core logic post with SEO and internal links`)
- Push: `origin/main` 반영 완료

### 검수 메모
- Front matter/내부 링크 문법 재확인
- 로컬 Jekyll 빌드: `bundle` 명령 부재로 미실행
- 개별 보고 생략, 일일 종합 보고에 포함 예정
