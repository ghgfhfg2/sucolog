## [TOOLOG_WEBTOOL_BUILD] 16:00 사이클
- 선정 툴: 평수 계산기 (`pyeong-calculator`) — ㎡↔평 변환 + 평당가 계산 수요 대응
- 구현:
  - `_tools/pyeong-calculator.md` 신규 추가 (SEO 메타/FAQ/내부링크 포함)
  - `_layouts/tool.html`에 `pyeong-calculator` 전용 UI 블록 추가
  - `assets/js/tools.js`에 양방향 변환/평당가/복사/초기화 로직 추가
  - `assets/thumbs/pyeong-calculator.svg` 썸네일 추가
  - `_data/tools.yml` 목록 등록
- SEO:
  - `_posts/2026-03-04-pyeong-calculator-core-logic.md` 발행 (핵심 로직 설명형 포스트)
- 검증:
  - `node -e "new Function(fs.readFileSync('assets/js/tools.js'))"` 파싱 검증 통과
  - 로컬 Jekyll 빌드는 `bundle` 미설치로 미실행
- 커밋:
  - `649c1b5` Add pyeong calculator tool with SEO content
  - `29642ec` Add SEO post for pyeong calculator logic

## [TOOLOG_WEBTOOL_IMPROVE] 16:30 사이클
- 대상: 평수 계산기 (`pyeong-calculator`) 재점검
- 점검/개선:
  - 예외 처리 강화: 숫자 형식이 아닌 입력 감지 시 안내 문구 명확화
  - UX 개선: 평당가가 큰 경우 `만원`과 함께 `억원` 보조 단위 동시 표기
  - 안내 문구 동기화: 결과 설명(help)에도 `억원` 보조 단위 반영
- 검증:
  - `node --check assets/js/tools.js` 문법 검증 통과
- 비고:
  - SEO 문구(만원/억원 보조 표시 제공)와 실제 동작 일치하도록 보정

## [TOOLOG_WEBTOOL_LOGIC_POST] 17:00 사이클
- 대상: 직전 추가 툴 `pyeong-calculator` 핵심 로직 설명글 발행 건 검수
- 조치:
  - `_posts/2026-03-04-pyeong-calculator-core-logic.md` H3 표기(`H3.` 접두어) 정리로 가독성 개선
  - 내부 링크/FAQ/메타 설명 구조 재확인 (유지)
- 검증:
  - `node --check assets/js/tools.js` 문법 검증 통과(회귀 확인)
- 배포:
  - 커밋 `5c0d2a8` (Polish pyeong calculator logic post headings)
  - `origin/main` 푸시 완료
- 보고:
  - 개별 전송 생략, 일일 종합 보고용 메모에만 기록
