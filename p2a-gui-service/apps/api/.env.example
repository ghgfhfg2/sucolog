PORT=4000
REDIS_URL=redis://localhost:6379
OUTPUT_DIR=./outputs

# Agent bridge mode: mock | webhook
AGENT_BRIDGE_MODE=mock

# Only required when AGENT_BRIDGE_MODE=webhook
# AGENT_BRIDGE_WEBHOOK_URL=http://localhost:8787/agent/generate
# AGENT_BRIDGE_BEARER_TOKEN=
